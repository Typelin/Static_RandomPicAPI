fix: 實現首頁與畫廊 UI 的絕對像素級統一

- 統一 `build.js` 中的全域 `:root` 變量、`max-width` 與 `padding`，消除頁面切換時的佈局跳動。
- 標準化 `nav-btn` 的 `stroke-width: 2` 與 `z-index`，解決圖示粗細不一與游標干擾問題。
- 實現主題切換按鈕在兩頁面的絕對座標一致 (967, 60)，達成 100% 零位移體驗。
- 優化畫廊頁面的左右導航對稱性，提升整體 Premium 質感。
